When a **tree** is in _symmetric order_, for a given node with key $n$ and two children with keys $a$ on the left and $b$ on the right, $n$ is always larger than $a$ but smaller than $b$.

![[Symmetric Order Image.png]]
